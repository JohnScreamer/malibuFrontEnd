import { DEFAULT_CATALOG_FILTERS } from "../constants/catalog.js";
import { FiltersKeys } from "../types/pages/catalogSearch.type.js";

type ToDefaultType = {};
export const useToDefaultBtnHandler = ({}: ToDefaultType) => {
    return {};
};
